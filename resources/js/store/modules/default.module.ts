const state = {
  message: {
    loading: false,
    valid: 0,
    color: 0,
    message: '',
    reset: false,
  },
};
const actions = {};
const mutations = {};
const getters = {
  defaults: (results: any) => results,
};

export default {
  state,
  actions,
  mutations,
  getters,
};
